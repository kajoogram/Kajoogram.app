[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  CI = "false"

# Essential for SPAs: Redirects all routes to index.html to allow internal routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200